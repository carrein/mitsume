# Copy to tooling/android-builder/.env (gitignored) and set the server URL that
# gets BAKED into the released APK — the deployed same-origin endpoint:
EXPO_PUBLIC_DAV_URL=https://your-host:port/dav/
