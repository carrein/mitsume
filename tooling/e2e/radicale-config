# Test-only Radicale for the e2e stack (compose.yml). Accepts any credentials —
# the Caddyfile injects "test:test", making /test/ the owned principal.
[server]
hosts = 0.0.0.0:5232

[auth]
type = none

[storage]
filesystem_folder = /data

[rights]
type = owner_only
