# macOS
.DS_Store

# Claude Code local settings
.claude/settings.local.json

# Node / Expo (app-level ignores live in app/.gitignore)
node_modules/

# Local env files
tooling/dev-proxy/.env
tooling/android-builder/.env

# Built APKs (attached to GitHub Releases, not committed)
dist-apk/
