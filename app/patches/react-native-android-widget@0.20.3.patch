diff --git a/node_modules/react-native-android-widget/.bun-tag-8b218605c9a901dc b/.bun-tag-8b218605c9a901dc
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/react-native-android-widget/.bun-tag-906bc642eebbaf32 b/.bun-tag-906bc642eebbaf32
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/react-native-android-widget/.bun-tag-99a6212790326dd9 b/.bun-tag-99a6212790326dd9
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/react-native-android-widget/.bun-tag-ac6aef17229dc530 b/.bun-tag-ac6aef17229dc530
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/android/src/main/java/com/reactnativeandroidwidget/RNWidget.java b/android/src/main/java/com/reactnativeandroidwidget/RNWidget.java
index 2d88172e426ccd70288005d4b7c15d7b534d20d9..c61353ad72abceeaaa5a0edffa0a10d71bd205e0 100644
--- a/android/src/main/java/com/reactnativeandroidwidget/RNWidget.java
+++ b/android/src/main/java/com/reactnativeandroidwidget/RNWidget.java
@@ -189,6 +189,39 @@ public class RNWidget {
     }
 
     private void registerClickTask(int id, ClickableView clickableView, RemoteViews widgetView, Integer button) {
+        // mitsume patch: OPEN_APP taps use a direct activity PendingIntent, like any
+        // native widget. The stock broadcast->receiver->startActivity path requires
+        // spawning the app process in the background, which OEM process killers
+        // (ColorOS "Hans"/prcl, MIUI) terminate before it can act (see upstream #108).
+        if ("OPEN_APP".equals(clickableView.getClickAction())) {
+            Intent launchIntent = appContext.getPackageManager()
+                .getLaunchIntentForPackage(appContext.getPackageName());
+            if (launchIntent != null) {
+                PendingIntent activityPendingIntent = PendingIntent.getActivity(
+                    appContext,
+                    button,
+                    launchIntent,
+                    PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE
+                );
+                widgetView.setOnClickPendingIntent(button, activityPendingIntent);
+                return;
+            }
+        }
+        // mitsume patch: same direct-PendingIntent treatment for OPEN_URI (see
+        // RNWidgetUtil.createOpenUriPendingIntent for the rationale).
+        if ("OPEN_URI".equals(clickableView.getClickAction())
+            && clickableView.getClickActionData() != null
+            && clickableView.getClickActionData().hasKey("uri")) {
+            PendingIntent uriPendingIntent = RNWidgetUtil.createOpenUriPendingIntent(
+                appContext,
+                clickableView.getClickActionData().getString("uri"),
+                button
+            );
+            if (uriPendingIntent != null) {
+                widgetView.setOnClickPendingIntent(button, uriPendingIntent);
+                return;
+            }
+        }
         Intent intent = new Intent(appContext.getPackageName() + ".WIDGET_CLICK");
         intent.setComponent(new ComponentName(appContext, RNWidgetUtil.getWidgetProviderClassName(appContext, widgetName)));
         intent.putExtra("widgetId", id);
diff --git a/android/src/main/java/com/reactnativeandroidwidget/RNWidgetCollectionService.java b/android/src/main/java/com/reactnativeandroidwidget/RNWidgetCollectionService.java
index 2bdbab9fe634d23e90a4b0b58d591de7745b18c2..ad27ab05bee61693fd1482ce3025b1efc5b66e12 100644
--- a/android/src/main/java/com/reactnativeandroidwidget/RNWidgetCollectionService.java
+++ b/android/src/main/java/com/reactnativeandroidwidget/RNWidgetCollectionService.java
@@ -143,6 +143,22 @@ class ListRemoteViewsFactory implements RemoteViewsService.RemoteViewsFactory {
     }
 
     private PendingIntent createPendingIntent(Bundle clickableArea) {
+        // mitsume patch: OPEN_URI taps in list items also get a direct ACTION_VIEW
+        // PendingIntent (see RNWidgetUtil.createOpenUriPendingIntent for rationale).
+        if ("OPEN_URI".equals(clickableArea.getString("clickAction"))) {
+            Bundle clickActionData = clickableArea.getBundle("clickActionData");
+            String uri = clickActionData != null ? clickActionData.getString("uri") : null;
+            if (uri != null) {
+                PendingIntent uriPendingIntent = RNWidgetUtil.createOpenUriPendingIntent(
+                    mContext,
+                    uri,
+                    (int) System.currentTimeMillis()
+                );
+                if (uriPendingIntent != null) {
+                    return uriPendingIntent;
+                }
+            }
+        }
         Intent intent = new Intent(mContext.getPackageName() + ".WIDGET_CLICK");
         intent.setComponent(new ComponentName(mContext, RNWidgetUtil.getWidgetProviderClassName(mContext, widgetName)));
         intent.putExtra("widgetId", mAppWidgetId);
diff --git a/android/src/main/java/com/reactnativeandroidwidget/RNWidgetUtil.java b/android/src/main/java/com/reactnativeandroidwidget/RNWidgetUtil.java
index a057c09736ee40464ca7d9d618079114bdd5518c..fcd56449ab2b36ec2f6af76d87a5058cfe16acb6 100644
--- a/android/src/main/java/com/reactnativeandroidwidget/RNWidgetUtil.java
+++ b/android/src/main/java/com/reactnativeandroidwidget/RNWidgetUtil.java
@@ -2,10 +2,13 @@ package com.reactnativeandroidwidget;
 
 import static android.content.res.Configuration.ORIENTATION_PORTRAIT;
 
+import android.app.PendingIntent;
 import android.appwidget.AppWidgetManager;
 import android.appwidget.AppWidgetProviderInfo;
 import android.content.ComponentName;
 import android.content.Context;
+import android.content.Intent;
+import android.net.Uri;
 import android.util.DisplayMetrics;
 import android.util.TypedValue;
 
@@ -17,22 +20,82 @@ import com.facebook.react.bridge.WritableMap;
 import java.util.List;
 
 public class RNWidgetUtil {
+    // mitsume patch: OPEN_URI taps use a direct ACTION_VIEW activity PendingIntent,
+    // like OPEN_APP. The stock broadcast->receiver->startActivity path requires
+    // spawning the app process in the background, which OEM process killers
+    // (ColorOS "Hans"/prcl, MIUI) terminate before it can act (see upstream #108).
+    // Own-app deep links (any URI this app resolves) stay pinned to this package so
+    // installing a second variant (com.carrein.mitsume.dev) never shows a chooser.
+    public static PendingIntent createOpenUriPendingIntent(Context context, String uri, int requestCode) {
+        try {
+            Intent intent = new Intent(Intent.ACTION_VIEW, Uri.parse(uri));
+            intent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
+            intent.setPackage(context.getPackageName());
+            if (intent.resolveActivity(context.getPackageManager()) == null) {
+                intent.setPackage(null);
+            }
+            return PendingIntent.getActivity(
+                context,
+                requestCode,
+                intent,
+                PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE
+            );
+        } catch (Exception e) {
+            return null;
+        }
+    }
+
     public static int getWidgetWidth(Context context, int widgetId) {
         boolean isPortrait = context.getResources().getConfiguration().orientation == ORIENTATION_PORTRAIT;
+        int width;
         if (isPortrait) {
-            return getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MIN_WIDTH);
+            width = getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MIN_WIDTH);
         } else {
-            return getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MAX_WIDTH);
+            width = getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MAX_WIDTH);
+        }
+        if (width == 0) {
+            width = getWidgetSizeInDp(context, widgetId, isPortrait
+                ? AppWidgetManager.OPTION_APPWIDGET_MAX_WIDTH
+                : AppWidgetManager.OPTION_APPWIDGET_MIN_WIDTH);
+        }
+        if (width == 0) {
+            width = getProviderFallbackSizeDp(context, widgetId, true);
         }
+        return width;
     }
 
     public static int getWidgetHeight(Context context, int widgetId) {
         boolean isPortrait = context.getResources().getConfiguration().orientation == ORIENTATION_PORTRAIT;
+        int height;
         if (isPortrait) {
-            return getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MAX_HEIGHT);
+            height = getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MAX_HEIGHT);
         } else {
-            return getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MIN_HEIGHT);
+            height = getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MIN_HEIGHT);
+        }
+        if (height == 0) {
+            height = getWidgetSizeInDp(context, widgetId, isPortrait
+                ? AppWidgetManager.OPTION_APPWIDGET_MIN_HEIGHT
+                : AppWidgetManager.OPTION_APPWIDGET_MAX_HEIGHT);
+        }
+        if (height == 0) {
+            height = getProviderFallbackSizeDp(context, widgetId, false);
+        }
+        return height;
+    }
+
+    // mitsume patch: launchers occasionally report zero size options for a bound
+    // widget (observed on Lawnchair after a home-screen rearrange, 2026-07-15) —
+    // stock RNAW then aborts every render ("ListWidget width and height must be
+    // > 0") until the widget is re-added. Fall back to the provider's declared
+    // minimum size so the widget keeps rendering; the next real options push
+    // from the launcher corrects the aspect.
+    private static int getProviderFallbackSizeDp(Context context, int widgetId, boolean wantWidth) {
+        AppWidgetProviderInfo info = AppWidgetManager.getInstance(context).getAppWidgetInfo(widgetId);
+        if (info == null) {
+            return 0;
         }
+        int px = wantWidth ? info.minWidth : info.minHeight;
+        return (int) pxToDp(context, px);
     }
 
     public static int dpToPx(Context context, double dpValue) {
